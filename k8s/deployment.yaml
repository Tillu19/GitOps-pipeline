apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-deploy
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: hanvitha/git-project-demo:v1
          ports:
            - containerPort: 80

